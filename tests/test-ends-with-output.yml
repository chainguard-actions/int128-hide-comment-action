name: Test ends-with output echoed back
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          token: ${{ github.token }}
          ends-with: "<!-- /bot-comment -->"
          issue-number: "1"
      - name: Assert ends-with output or graceful failure
        env:
          OUTCOME: ${{ steps.action.outcome }}
          ENDS_WITH: ${{ steps.action.outputs.ends-with }}
        run: |
          echo "outcome: $OUTCOME"
          echo "ends-with output: $ENDS_WITH"
          if [ "$OUTCOME" = "success" ]; then
            if [ "$ENDS_WITH" != "<!-- /bot-comment -->" ]; then
              echo "FAIL: expected ends-with output to be '<!-- /bot-comment -->' but got '$ENDS_WITH'"
              exit 1
            fi
            echo "PASS: ends-with output matches input"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (sentinel repo is not resolvable)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
